{
  "name": "@eulerstream/overlay-cli",
  "version": "0.1.0",
  "description": "CLI for building TikTok Live overlays",
  "type": "module",
  "bin": {
    "overlay-cli": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --packages=external",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@types/archiver": "^7.0.0",
    "archiver": "^7.0.1",
    "commander": "^12.0.0",
    "esbuild": "^0.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
